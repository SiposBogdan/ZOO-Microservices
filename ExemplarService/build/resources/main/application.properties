spring.application.name=ExemplarService
animal.service.url=http://localhost:8081

spring.datasource.url=jdbc:mysql://localhost:3306/exemplar_db

spring.datasource.username=root
spring.datasource.password=MySQL

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8084

# a 256-bit key, Base64-encoded (you can generate one with e.g. openssl rand -base64 32)
# Base64-encoded 256-bit secret
# Base-64 256-bit secret
jwt.secret=YOUR_BASE64_256BIT_SECRET

# Token lifetime in ms
jwt.expiration=86400000
animal.service.token=YOUR_BASE64_256BIT_SECRET

# URL (including protocol and port) of your AnimalService

# A valid JWT that the AnimalService?s JwtAuthenticationFilter will accept
